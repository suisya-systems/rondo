# The lockfile is the pinned build input (DECISIONS.md D-0007): CI installs with
# `npm ci`, which refuses to proceed when package.json and package-lock.json
# disagree. save-exact keeps the manifest honest about what was resolved.
save-exact=true
fund=false
audit=false
